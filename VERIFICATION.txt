OSU!BAND source verification
============================

Base: known-working LAME 2026.804.2 clean runtime port.

Static checks performed in the build workspace:
- all .vcxproj XML parsed successfully;
- every ClCompile / ClInclude path referenced by the projects exists;
- no osu!stable client / offsets / client-kind references remain;
- braces/parentheses balanced in the main changed runtime/UI sources;
- Stable gameplay surface is limited to Aim Assist, Relax and Replay.
- AutoBot and TapAssist source directories, project entries, settings fields and hidden UI branches were removed from Stable.
- Cloud config serialization in Stable no longer emits AutoBot/TapAssist keys.
- Stable/Beta channel filtering remains server-enforced.


Runtime/Windows build note:
This environment does not contain MSVC + the Windows SDK, so the source could not be compiled or executed here. A GitHub Actions workflow is included to perform the real Release x64 build on Windows.
